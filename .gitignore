/target/
**/*.rs.bk
.DS_Store

# depthai-core source is a pinned git submodule at vendor/depthai-core (tracked).
# The built install prefix + packaged tarball are large build output — not committed.
/vendor/depthai/
/vendor/*.tar.gz

# pixi / local
.pixi/
