/target/
**/*.rs.bk
.DS_Store

# A local depthai-core install prefix (point DEPTHAI_PREFIX at it) — build output,
# not committed. The source and build script live in kornia/depthai-rs now.
/vendor/

# pixi / local
.pixi/

# depthai zoo/telemetry cache — not committed.
/.cache/
.worktrees/
