[package]
name = "sensor-types"
version.workspace = true
edition.workspace = true
repository.workspace = true
license.workspace = true
publish = false
description = "Frame-timing types shared by every sensor-rt driver: frame metadata and stamped frames"

[lib]
name = "sensor_types"

[dependencies]
